export enum TrackOpType {
    GET = 'get', // 获取
    HAS = 'has', // 是否存在
    ITERATE = 'iterate' // 遍历
}

export enum TriggerOpType {
    SET = 'set', // 设置
    ADD = 'add', // 添加
    DELETE = 'delete' // 删除
}
